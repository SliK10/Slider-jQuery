import './index.scss'

interface User {
  name: string;
  id: number;
}

const user: User = {
  name: 'Helga',
  id: 0,
};

// console.log(JSON.stringify(user));

// console.log('Name of user: ' + user.name);
// console.log('Id user: ' + user.id);
